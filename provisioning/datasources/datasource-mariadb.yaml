apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    access: proxy
    url: ${MYSQL_HOST}
    database: "geoip"
    user: "geoip"
    secureJsonData:
      password: "${MYSQL_PASSWORD}"
    isDefault: true
    editable: true
