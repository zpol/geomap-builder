apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    access: proxy
    url: ${MYSQL_HOST}
    database: "geoip" #TODO
    user: "geoip" #TODO
    secureJsonData:
      password: "${MYSQL_PASSWORD}"
    isDefault: true
    editable: true
